import { theme } from "../tailwind.config.js";
import { Meta } from "@storybook/addon-docs";
import defaultConfig from "tailwindcss/defaultConfig";
import resolveConfig from "tailwindcss/resolveConfig";

<Meta title="Framework/Typography" />

# Typography

<style>
  {`    
.size {
  display: block;
  font-style: normal !important;
  font-weight: normal !important;
  color: #333;
  margin-bottom: 6px !important;
}

.text-preview {
  padding: 0 !important;
  overflow: hidden;
  text-overflow: ellipsis;
  width: 100%;
  white-space: nowrap;
  font-family: Circular, Georgia;
}
`}
</style>

<div style={{ marginBottom: 100 }}>
  {Object.entries(resolveConfig([theme, defaultConfig]).fontSize)
    .filter(([key, value]) => key !== "0")
    .map(([key, value]) => {
      console.log(key, value);
      return (
        <dl key={key} style={{ marginBottom: 40 }}>
          <dt className="size">
            {key} - {value}
          </dt>
          {[400, 500, 600, 700].map((fontWeight) => (
            <dd
              key={fontWeight}
              className="text-preview"
              style={{
                fontSize: Array.isArray(value) ? value[0] : value,
                lineHeight: Array.isArray(value) ? value[1] : 1,
                fontWeight,
              }}
            >
              The quick brown fox jumped over the lazy dog.
            </dd>
          ))}
        </dl>
      );
    })}
</div>
